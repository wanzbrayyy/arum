<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>WARKOP ARUM DALU</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Playfair+Display:wght@700&family=Montserrat:wght@400;500;600&display=swap');

        :root {
            --primary-dark: #2c3e50;
            --secondary-dark: #34495e;
            --accent-green: #27ae60;
            --text-light: #ecf0f1;
            --text-dark: #bdc3c7;
            --border-color: #4a627a;
        }

        body {
            font-family: 'Montserrat', sans-serif;
            margin: 0;
            padding: 0;
            background-color: var(--primary-dark);
            color: var(--text-light);
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            overflow-x: hidden; /* Mencegah overflow horizontal */
            background-image: url('https://i.imgur.com/your-cafe-background-image.jpg'); /* Placeholder */
            background-size: cover;
            background-position: center;
            background-attachment: fixed;
            position: relative;
        }

        body::before {
            content: "";
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: rgba(0, 0, 0, 0.7);
            z-index: 1;
        }

        .container {
            background-color: var(--secondary-dark);
            border-radius: 12px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.4);
            padding: 20px; /* Kurangi padding untuk mobile */
            width: 100%;
            max-width: 1200px;
            z-index: 2;
            display: flex;
            gap: 20px;
            min-height: 600px;
            position: relative;
            flex-direction: row; /* Default desktop */
        }

        .auth-card {
            background-color: var(--secondary-dark);
            border-radius: 12px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.4);
            padding: 30px; /* Kurangi padding untuk mobile */
            width: 100%;
            max-width: 500px;
            z-index: 2;
            text-align: center;
        }

        h1, h2, h3 {
            font-family: 'Playfair Display', serif;
            color: var(--text-light);
            margin-bottom: 20px;
        }

        .logo-title {
            font-family: 'Playfair Display', serif;
            font-size: 2.2em; /* Sesuaikan ukuran font */
            color: var(--text-light);
            margin-bottom: 5px;
            position: relative;
            display: inline-block;
            text-shadow: 2px 2px 5px rgba(0,0,0,0.5);
        }

        .logo-title::after {
            content: '™';
            font-size: 0.5em;
            vertical-align: super;
            position: absolute;
            top: 0;
            right: -20px;
            color: var(--accent-green);
        }

        .logo-subtitle {
            font-family: 'Montserrat', sans-serif;
            font-size: 0.8em; /* Sesuaikan ukuran font */
            color: var(--text-dark);
            margin-bottom: 25px; /* Kurangi margin */
        }

        input[type="text"],
        input[type="password"],
        input[type="number"],
        select {
            width: calc(100% - 30px);
            padding: 10px 15px; /* Kurangi padding */
            margin-bottom: 15px; /* Kurangi margin */
            border: 1px solid var(--border-color);
            border-radius: 8px;
            background-color: var(--primary-dark);
            color: var(--text-light);
            font-size: 0.9em; /* Sesuaikan ukuran font */
            box-sizing: border-box;
            transition: border-color 0.3s ease;
        }

        input[type="text"]:focus,
        input[type="password"]:focus,
        input[type="number"]:focus,
        select:focus {
            border-color: var(--accent-green);
            outline: none;
        }

        .form-group {
            position: relative;
            margin-bottom: 15px; /* Kurangi margin */
        }

        .form-group i {
            position: absolute;
            left: 15px;
            top: 50%;
            transform: translateY(-50%);
            color: var(--text-dark);
        }

        .form-group input {
            padding-left: 40px; /* Kurangi padding */
        }

        .btn {
            background-color: var(--accent-green);
            color: white;
            padding: 10px 20px; /* Kurangi padding */
            border: none;
            border-radius: 8px;
            cursor: pointer;
            font-size: 1em; /* Sesuaikan ukuran font */
            transition: background-color 0.3s ease, transform 0.2s ease;
            width: 100%;
            box-sizing: border-box;
        }

        .btn:hover {
            background-color: #219d53;
            transform: translateY(-2px);
        }

        .btn:active {
            transform: translateY(0);
        }

        .link {
            color: var(--accent-green);
            text-decoration: none;
            font-size: 0.8em; /* Sesuaikan ukuran font */
            margin-top: 10px; /* Kurangi margin */
            display: inline-block;
            transition: color 0.3s ease;
        }

        .link:hover {
            color: #219d53;
        }

        .sidebar {
            width: 250px;
            background-color: var(--primary-dark);
            border-radius: 10px;
            padding: 20px;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.3);
            display: flex;
            flex-direction: column;
            justify-content: space-between;
            flex-shrink: 0; /* Mencegah sidebar menyusut terlalu banyak */
        }

        .sidebar .logo {
            text-align: center;
            margin-bottom: 30px; /* Kurangi margin */
        }

        .sidebar .logo .logo-title {
            font-size: 1.8em; /* Sesuaikan ukuran font */
            margin-bottom: 5px;
        }

        .sidebar .logo .logo-subtitle {
            font-size: 0.7em; /* Sesuaikan ukuran font */
        }

        .sidebar-menu ul {
            list-style: none;
            padding: 0;
            margin: 0;
        }

        .sidebar-menu ul li {
            margin-bottom: 10px; /* Kurangi margin */
        }

        .sidebar-menu ul li a {
            color: var(--text-dark);
            text-decoration: none;
            display: block;
            padding: 8px 12px; /* Kurangi padding */
            border-radius: 8px;
            transition: background-color 0.3s ease, color 0.3s ease;
            font-size: 0.9em; /* Sesuaikan ukuran font */
        }

        .sidebar-menu ul li a:hover,
        .sidebar-menu ul li a.active {
            background-color: var(--secondary-dark);
            color: var(--text-light);
        }

        .sidebar-menu ul li a i {
            margin-right: 8px; /* Kurangi margin */
            width: 18px; /* Sesuaikan lebar ikon */
            text-align: center;
        }

        .main-content {
            flex-grow: 1;
            background-color: var(--primary-dark);
            border-radius: 10px;
            padding: 20px; /* Kurangi padding */
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.3);
            overflow-x: auto; /* Memungkinkan tabel atau konten lebar untuk di-scroll */
        }

        .dashboard-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 25px; /* Kurangi margin */
            padding-bottom: 10px; /* Kurangi padding */
            border-bottom: 1px solid var(--border-color);
            flex-wrap: wrap; /* Memungkinkan wrap pada mobile */
        }

        .dashboard-header h2 {
            margin: 0;
            font-size: 1.5em; /* Sesuaikan ukuran font */
            color: var(--text-light);
        }

        .dashboard-header .user-info {
            display: flex;
            align-items: center;
            margin-top: 10px; /* Untuk wrap pada mobile */
        }

        .dashboard-header .user-info span {
            margin-right: 8px; /* Kurangi margin */
            color: var(--text-dark);
            font-size: 0.9em; /* Sesuaikan ukuran font */
        }

        .dashboard-header .user-info i {
            font-size: 1.1em; /* Sesuaikan ukuran font */
            color: var(--accent-green);
        }

        .order-panel {
            display: flex;
            gap: 20px;
            flex-direction: row; /* Default desktop */
        }

        .order-list {
            flex: 2;
            background-color: var(--secondary-dark);
            border-radius: 10px;
            padding: 15px; /* Kurangi padding */
            overflow-x: auto; /* Untuk tabel di dalam */
        }

        .order-list h3 {
            margin-top: 0;
            border-bottom: 1px solid var(--border-color);
            padding-bottom: 8px; /* Kurangi padding */
            margin-bottom: 15px; /* Kurangi margin */
            font-size: 1.2em; /* Sesuaikan ukuran font */
        }

        .order-table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 15px; /* Kurangi margin */
            min-width: 400px; /* Minimal lebar tabel untuk mencegah terlalu sempit */
        }

        .order-table th, .order-table td {
            text-align: left;
            padding: 10px; /* Kurangi padding */
            border-bottom: 1px solid var(--border-color);
            font-size: 0.9em; /* Sesuaikan ukuran font */
        }

        .order-table th {
            color: var(--text-dark);
            font-weight: 600;
        }

        .order-table td {
            color: var(--text-light);
        }

        .subtotal {
            font-size: 1.2em; /* Sesuaikan ukuran font */
            font-weight: 600;
            color: var(--accent-green);
            text-align: right;
            margin-top: 15px; /* Kurangi margin */
        }

        .add-product-section {
            flex: 1;
            background-color: var(--secondary-dark);
            border-radius: 10px;
            padding: 15px; /* Kurangi padding */
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
        }

        .add-product-section .btn-add-product {
            width: 100px; /* Kurangi ukuran tombol */
            height: 100px; /* Kurangi ukuran tombol */
            font-size: 2.5em; /* Kurangi ukuran font */
            margin-bottom: 15px; /* Kurangi margin */
        }

        .category-filter {
            display: flex;
            flex-wrap: wrap; /* Memungkinkan wrap pada mobile */
            gap: 8px; /* Kurangi gap */
            margin-top: 15px; /* Kurangi margin */
            justify-content: center; /* Pusatkan kategori filter */
        }

        .category-btn {
            background-color: var(--primary-dark);
            color: var(--text-dark);
            padding: 8px 15px; /* Kurangi padding */
            border-radius: 20px; /* Kurangi border-radius */
            border: 1px solid var(--border-color);
            cursor: pointer;
            transition: background-color 0.3s ease, color 0.3s ease;
            display: flex;
            align-items: center;
            font-size: 0.8em; /* Sesuaikan ukuran font */
        }

        .category-btn i {
            margin-right: 5px; /* Kurangi margin */
        }

        .category-btn.active,
        .category-btn:hover {
            background-color: var(--accent-green);
            color: white;
            border-color: var(--accent-green);
        }

        .modal {
            display: none;
            position: fixed;
            z-index: 100;
            left: 0;
            top: 0;
            width: 100%;
            height: 100%;
            overflow: auto;
            background-color: rgba(0,0,0,0.6);
            justify-content: center;
            align-items: center;
        }

        .modal-content {
            background-color: var(--secondary-dark);
            margin: auto;
            padding: 25px; /* Kurangi padding */
            border-radius: 12px;
            width: 95%; /* Lebih lebar di mobile */
            max-width: 550px; /* Batasi lebar maksimum */
            box-shadow: 0 10px 30px rgba(0,0,0,0.5);
            position: relative;
        }

        .close-button {
            color: var(--text-dark);
            position: absolute;
            top: 10px; /* Sesuaikan posisi */
            right: 20px; /* Sesuaikan posisi */
            font-size: 24px; /* Sesuaikan ukuran font */
            font-weight: bold;
            cursor: pointer;
        }

        .modal-body {
            max-height: 350px; /* Kurangi tinggi maksimum */
            overflow-y: auto;
            padding-right: 10px; /* Kurangi padding */
        }

        .modal-product-item {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 8px 0; /* Kurangi padding */
            border-bottom: 1px solid var(--border-color);
            flex-wrap: wrap; /* Memungkinkan wrap pada mobile */
        }

        .modal-product-item label {
            flex-grow: 1;
            cursor: pointer;
            color: var(--text-light);
            font-size: 0.9em; /* Sesuaikan ukuran font */
            min-width: 150px; /* Agar nama produk tidak terlalu kecil */
        }

        .modal-product-item input[type="checkbox"] {
            margin-right: 10px; /* Kurangi margin */
            transform: scale(1.1); /* Sedikit lebih besar */
        }

        .modal-product-item input[type="number"] {
            width: 70px; /* Kurangi lebar */
            margin-left: 10px; /* Kurangi margin */
            font-size: 0.9em; /* Sesuaikan ukuran font */
            padding: 5px; /* Kurangi padding */
        }

        .modal-footer {
            margin-top: 15px; /* Kurangi margin */
            text-align: right;
        }

        .alert-message {
            background-color: #e74c3c;
            color: white;
            padding: 10px;
            border-radius: 5px;
            margin-bottom: 15px;
            text-align: center;
            font-size: 0.9em; /* Sesuaikan ukuran font */
        }

        /* Responsive Adjustments */
        @media (max-width: 768px) {
            body {
                align-items: flex-start; /* Untuk konten di atas di mobile */
                padding: 10px;
            }

            .container {
                flex-direction: column; /* Stack sidebar dan main content */
                min-height: auto;
                padding: 15px;
            }

            .auth-card {
                padding: 25px;
            }

            .sidebar {
                width: 100%;
                margin-bottom: 20px;
                padding: 15px;
            }

            .sidebar .logo .logo-title {
                font-size: 2em;
            }

            .sidebar .logo .logo-subtitle {
                font-size: 0.75em;
            }

            .sidebar-menu ul {
                display: flex;
                flex-wrap: wrap;
                justify-content: center;
                gap: 5px;
            }

            .sidebar-menu ul li {
                margin-bottom: 0;
            }

            .sidebar-menu ul li a {
                padding: 6px 10px;
                font-size: 0.8em;
                white-space: nowrap; /* Mencegah wrap pada item menu */
            }

            .sidebar-menu ul li a i {
                margin-right: 5px;
            }

            .main-content {
                padding: 15px;
            }

            .dashboard-header {
                flex-direction: column;
                align-items: flex-start;
                text-align: left;
                margin-bottom: 20px;
            }

            .dashboard-header h2 {
                font-size: 1.4em;
                margin-bottom: 10px;
            }

            .dashboard-header .user-info {
                margin-top: 0;
            }

            .order-panel {
                flex-direction: column; /* Stack order list dan add product section */
            }

            .order-list {
                width: 100%; /* Lebar penuh */
            }

            .add-product-section {
                width: 100%; /* Lebar penuh */
                margin-top: 20px;
            }

            .order-table th, .order-table td {
                padding: 8px;
                font-size: 0.8em;
            }

            .order-table th:nth-child(1), .order-table td:nth-child(1) { /* Product Name */
                width: 40%;
            }
            .order-table th:nth-child(2), .order-table td:nth-child(2) { /* Qty */
                width: 20%;
            }
            .order-table th:nth-child(3), .order-table td:nth-child(3) { /* Price */
                width: 30%;
            }
            .order-table th:nth-child(4), .order-table td:nth-child(4) { /* Action */
                width: 10%;
            }

            .btn-add-product {
                width: 80px;
                height: 80px;
                font-size: 2em;
            }

            .category-filter {
                gap: 5px;
            }

            .category-btn {
                padding: 6px 10px;
                font-size: 0.75em;
            }

            .modal-content {
                width: 95%;
                padding: 20px;
            }

            .modal-product-item {
                flex-direction: column;
                align-items: flex-start;
                gap: 5px;
            }

            .modal-product-item label {
                width: 100%;
                margin-bottom: 5px;
            }

            .modal-product-item input[type="checkbox"] {
                margin-right: 0;
            }

            .modal-product-item input[type="number"] {
                margin-left: 0;
                width: 100%; /* Lebar penuh di mobile */
            }
        }

        @media (max-width: 480px) {
            .logo-title {
                font-size: 1.8em;
            }
            .logo-subtitle {
                font-size: 0.7em;
            }
            .sidebar-menu ul {
                justify-content: space-around;
            }
            .sidebar-menu ul li a {
                font-size: 0.7em;
                padding: 5px 8px;
            }
            .order-table th, .order-table td {
                font-size: 0.75em;
            }
            .subtotal {
                font-size: 1.1em;
            }
            .btn {
                font-size: 0.9em;
                padding: 8px 15px;
            }
            .category-btn {
                font-size: 0.7em;
                padding: 5px 10px;
            }
            .modal-product-item label {
                font-size: 0.8em;
            }
        }

    </style>
</head>
<body>